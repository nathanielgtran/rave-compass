; rave-compass firmware — PlatformIO project config
; Three environments:
;   native          — host unit tests (Unity framework, no hardware required)
;   l1lite_token    — nRF52840 Wio Tracker L1 Lite, ROLE_TOKEN
;   l1lite_beacon   — nRF52840 Wio Tracker L1 Lite, ROLE_BEACON
;
; Acceptance gate: pio test -e native

[env:native]
platform = native
test_framework = unity
build_flags =
    -std=c++17
    -DNATIVE_TEST
    -DUNITY_INCLUDE_DOUBLE
; Exclude src/ (main.cpp) from native build — it pulls hardware headers
build_src_filter = -<src/>

; TODO: switch to Wio Tracker L1 Lite board def when hardware arrives + verified
[env:l1lite_token]
platform = nordicnrf52
board = seeed_xiao_nrf52840
framework = arduino
build_flags =
    -DROLE_TOKEN
    -std=c++17
lib_deps =
    jgromes/RadioLib

; TODO: switch to Wio Tracker L1 Lite board def when hardware arrives + verified
[env:l1lite_beacon]
platform = nordicnrf52
board = seeed_xiao_nrf52840
framework = arduino
build_flags =
    -DROLE_BEACON
    -std=c++17
lib_deps =
    jgromes/RadioLib
